# Calculate and print the number of key/value pairs in the following dictionary:

car = {
    'type':  'sedan',
    'color': 'blue',
    'year':  2003,
}

# print(len(car.keys()))

# Above is a unnecessary step can just call len() on the dict itself

print(len(car))
